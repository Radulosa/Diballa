if y > 1500 {

	image_speed = 0.7

	if image_index >= lastFrame {
	image_speed = 0
	}
}

if image_alpha <=0 instance_destroy()
if room == room_powers {
	if !place_meeting(x,y,oPointer) {
	if image_alpha > 0.3
		image_alpha -= 0.03
	}
	if image_alpha >= 1 image_alpha = 1
}